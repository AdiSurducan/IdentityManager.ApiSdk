meta {
  name: Auth
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/imx/login/portal
  body: json
  auth: inherit
}

body:json {
  {
    "Module": "{{authMethod}}",
    "User":"{{username}}",
    "Password":"{{password}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
